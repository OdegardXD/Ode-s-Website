function checkDeviceAndRunCode() {
  // Define a breakpoint for mobile devices (e.g., 768px) ty google for the code
  const mobileBreakpoint = 600;

  // Check if the current window width is less than the mobile breakpoint again ty google for the code
  if (window.innerWidth < mobileBreakpoint) {
    console.log("why are u here on mobile? or why are u resizing ur window to be this small?");
  } else {

  }
}
checkDeviceAndRunCode();